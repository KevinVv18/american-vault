{
  "name": "american-vault",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Catalogo de carteras importadas — American Vault",
  "scripts": {
    "build": "node scripts/build-static.mjs",
    "seed": "node scripts/seed.mjs",
    "seed:dry": "node scripts/seed.mjs --dry-run"
  },
  "devDependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "dotenv": "^16.4.5"
  }
}
