# Copia este archivo como .env.local y rellena los valores.
# .env.local está en .gitignore — NUNCA lo commitees.

# URL del proyecto Supabase (segura, puede ir en cualquier lado)
SUPABASE_URL=https://acmllkuqsxukxevunzei.supabase.co

# Service Role Key — SECRETA. Solo se usa en scripts locales (seed).
# La sacas de: Supabase Dashboard > Project Settings > API > service_role (secret)
# NO la pongas en el frontend ni en GitHub.
SUPABASE_SERVICE_ROLE_KEY=
